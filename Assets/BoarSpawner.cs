using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoarSpawner : MonoBehaviour
{
    [Range(2, 6)]
    [SerializeField] private int maximumBoardSpawned;
    // Start is called before the first frame update
}
